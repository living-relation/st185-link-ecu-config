# CLAUDE.md

**Read [`AGENTS.md`](AGENTS.md). It is canonical.**

This file exists only so Claude Code loads the same standing rules that Cursor,
Codex and every other tool read from `AGENTS.md`. The rules are deliberately
**not** duplicated here — one rule kept in two files is one rule that will
eventually disagree with itself.
