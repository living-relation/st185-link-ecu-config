# Cursor sub-versions — reference only, NOT a source of truth

Everything in this folder was generated by a Cursor agent that had **no
harness.design access**. It could not open or draw in the app, so these are
script-generated sub-versions of our diagrams. They do not replace anything in
`docs/harness/`.

| File | Sub-version of |
|---|---|
| `ST185-cursor-Power.harness` | `../ST185-Power.harness` — **ours wins** |
| `ST185-cursor-EngineRoom-C-heavy-dc.harness` | split experiment against `../ST185-EngineRoom-C.harness` |
| `ST185-cursor-EngineRoom-C-loads.harness` | same |
| `ST185-cursor-EngineRoom-C-oem-inject.harness` | same |
| `gen_cursor_engine_room_c.py` | generator for the three above; writes into this folder |

## Rules for agents

- Never read these as a source of truth. The chain is in
  `docs/RECONCILIATION-RULES.md`: `XTREMEX-IO-TABLE.html` → `SCHEMATIC-WIRING.html`
  + `apps/harness-schematic/` → `docs/harness/*.harness` → harness.design mirror.
- Never feed these to `buildlist.py` or `buylist.py`. Both read explicit
  filenames in `docs/harness/` and must keep ignoring this folder.
- Mine them for facts and part numbers if useful, then land the fact in the real
  file. Do not copy geometry or layout out of them.
- Delete a file here once its content has been folded into the real diagram.
