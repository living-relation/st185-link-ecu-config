electrical: bring the engine-room redistribution doc current, flag the 6.43 clash

That document was last touched 2026-09-17, before 6.38, 6.41 and 6.43 were
written, and it had drifted.

Fixed, factually:

- Section 6 named ST185-Power.harness and ST185-Signal.harness as the drawings.
  Repointed at the eight rebuild/ looms, with legacy-prebuild/ named as frozen.
- The closing paragraph warned that Power still carried a leftover bh_c bulkhead
  with unassigned ETB / injector / coil 12 V on it. That is done - bh_c is gone
  from every harness file, verified. Marked closed instead of left as a warning.
- It also said the harness.design MCP was not connected. It is.

Flagged, not fixed - new section 10:

6.43 says the PDU owns most internal low-power and accessory circuits. Section
3.1 of this document keeps wiper, gauge, turn, CIG, ECU-IG, IGN, STOP, TAIL,
ECU-B and DEFOGGER on J/B No.1 fuses, and the heater and window circuits in the
kick-panel R/Bs. Those are the same circuits. Both cannot be right.

The channel count settles half of it on its own. A PMU-16 has 16 outputs. Five
are committed to the body channels in 3.1, three to the CAN devices in 6.38, one
to the battery-kill coil in 6.43 - nine used, seven free. J/B No.1 alone is about
ten circuits, so 6.43's "most" cannot mean all of them whatever Daniel decides.

Three options written up with that arithmetic. Until one is picked, 3.1 stands,
because it is the one that is fully specified and fits the hardware. Cross-linked
from 6.43's open list.

Also removed the last trace of the solid-state relay mix-up - Daniel confirmed he
has none.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01HnHFco3imTn3BeYxkAK9YR
