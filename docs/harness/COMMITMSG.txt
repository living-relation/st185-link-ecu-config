Add v0.9 lint, fix dangling parts, normalize striped wire colours

lint_v09.py checks every rebuild file against the schema rules we have
learned by rejection, so an upload stops being a guess. It found two real
bugs that would also have made the BOMs come up short:

- A-ECU referenced resistor parts rp_1k8 / rp_470 / rp_10k that were never
  defined. The cam pull-up, fuel-level pull-up and cruise-ladder pull-up
  had no part behind them.
- EngineRoom-C referenced connector part cp_mrs_ctrl, also undefined.

Both added in fix_missing_parts.py. Lint is now clean on all eight files.

split_colors() in layout_633.py: v0.9 has no striped wire colours, so
"Green/Yellow" is a hard reject. 24 wires across five files carried one.
The stripe now goes in its own stripeColor field.

layout_633.py also places resistors, following each one's locationId so it
sits beside the part it is fitted at, and drops stale wiringTable positions.

verify_rebuild.py still reports MATCH - 267 wires in, 259 out, every
connection accounted for.

Uploaded: ST185-EngineRoom-C = nz65, ST185-A-ECU = oOXY.

Known open: ecu_com cavity C4 carries CAN L (w_can_bh1_c) but is still
marked notConnected. Needs fixing before the CAN file is trusted.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01HnHFco3imTn3BeYxkAK9YR
